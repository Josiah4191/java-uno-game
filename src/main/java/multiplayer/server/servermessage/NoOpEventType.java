package multiplayer.server.servermessage;

/*
    This enum defines the no operation event types to identify why a no operation event took place
 */

public enum NoOpEventType {
    INVALID_CARD, INVALID_CALL_UNO, INVALID_TURN
}
